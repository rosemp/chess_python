def print_board(current_board):
    # Should update this to print board with a grid
    print("Current chess board:")
    for i in range(len(current_board)):
        print(current_board[i])
    
def parse_input_coords():
    # This function should return the a 2-element list which contains the
    # row and column index of the chess piece or the square the player wants
    # to move it to. 
    
    # Also add checks to make sure the desired input is in-bounds.
    
    # Game should end if a player enters "forfeit"
    pass

